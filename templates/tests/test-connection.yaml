apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "generic-app.fullname" . }}-test-connection"
  labels:
    {{- include "generic-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "generic-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
