---
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "docker-dind-sshd.fullname" . }}-helper-scripts
  labels:
    {{- include "docker-dind-sshd.labels" . | nindent 4 }}
data:
{{- with .Files.Glob "files/scripts/*.sh" }}
{{ tpl .AsConfig $ | indent 2 }}
{{- end }}

